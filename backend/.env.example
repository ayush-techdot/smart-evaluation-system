PORT=5000
MONGO_URI=mongodb://localhost:27017/smart-eval
JWT_SECRET=your_super_secret_jwt_key_change_this
GEMINI_API_KEY=your_gemini_api_key_here

# OCR_MODE: "mock" uses simulated text (for demo), "google" uses Cloud Vision
OCR_MODE=mock
# GOOGLE_APPLICATION_CREDENTIALS=./google-key.json  # only if OCR_MODE=google
